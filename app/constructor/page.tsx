import { ConstructorClient } from './ConstructorClient';

export const metadata = {
  title: 'GlassBlob Constructor — design your blob',
};

export default function ConstructorPage() {
  return <ConstructorClient />;
}
